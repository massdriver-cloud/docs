---
pagination_next: null
pagination_prev: null
hide_table_of_contents: true
id: organizations-sort-field
title: OrganizationsSortField
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








Available fields for sorting organizations.


```graphql
enum OrganizationsSortField {
  NAME
  CREATED_AT
}
```




### Values

#### [<code style={{ fontWeight: 'normal' }}>OrganizationsSortField.<b>NAME</b></code>](#name) \{#name\} 
Sort alphabetically by organization name (A-Z or Z-A).


#### [<code style={{ fontWeight: 'normal' }}>OrganizationsSortField.<b>CREATED&#x005F;AT</b></code>](#created-at) \{#created-at\} 
Sort by when the organization was created (oldest or newest first).






### Member Of

[`OrganizationsSort`](/api/graphql/types/inputs/organizations-sort.mdx)  <Badge class="badge badge--secondary badge--relation" text="input"/>