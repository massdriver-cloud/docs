---
pagination_next: null
pagination_prev: null
hide_table_of_contents: true
id: oci-repos
title: ociRepos
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








List OCI repositories in your organization's bundle catalog.

Returns a paginated list of repositories. Each repository is the container
for all published versions of a bundle. Use `filter` to narrow by name,
artifact type, or full-text search.

```graphql
query {
  ociRepos(organizationId: "your-org-id", filter: { name: { startsWith: "aws-" } }) {
    items {
      id
      name
      tags(sort: { field: VERSION, order: DESC }) {
        items { tag }
      }
    }
    cursor { next }
  }
}
```


```graphql
ociRepos(
  organizationId: ID!
  filter: OciReposFilter
  sort: OciReposSort
  cursor: Cursor
): OciReposPage
```




### Arguments

#### [<code style={{ fontWeight: 'normal' }}>ociRepos.<b>organizationId</b></code>](#organization-id)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>ID!</b></code>](/api/graphql/v1/types/scalars/id.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/> \{#organization-id\} 
Your organization's unique identifier.


#### [<code style={{ fontWeight: 'normal' }}>ociRepos.<b>filter</b></code>](#filter)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>OciReposFilter</b></code>](/api/graphql/v1/types/inputs/oci-repos-filter.mdx) <Badge class="badge badge--secondary " text="input"/> \{#filter\} 
Narrow results by name, artifact type, or search term.


#### [<code style={{ fontWeight: 'normal' }}>ociRepos.<b>sort</b></code>](#sort)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>OciReposSort</b></code>](/api/graphql/v1/types/inputs/oci-repos-sort.mdx) <Badge class="badge badge--secondary " text="input"/> \{#sort\} 
Explicit sort order. Defaults to `name` ascending. When a `search` filter is active and no sort is provided, results are ranked by relevance.


#### [<code style={{ fontWeight: 'normal' }}>ociRepos.<b>cursor</b></code>](#cursor)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Cursor</b></code>](/api/graphql/v1/types/inputs/cursor.mdx) <Badge class="badge badge--secondary " text="input"/> \{#cursor\} 
Cursor from a previous page's `cursor.next` or `cursor.previous`.


### Type

#### [<code style={{ fontWeight: 'normal' }}><b>OciReposPage</b></code>](/api/graphql/v1/types/objects/oci-repos-page.mdx) <Badge class="badge badge--secondary " text="object"/>