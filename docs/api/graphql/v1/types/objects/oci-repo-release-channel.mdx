---
pagination_next: null
pagination_prev: null
hide_table_of_contents: true
id: oci-repo-release-channel
title: OciRepoReleaseChannel
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








A release channel within an OCI repository.

Release channels are auto-resolving version constraints. Each channel points to
the latest published tag that matches its constraint. As new versions are
published, channels automatically update to point to the newest match.

For example, channel `~1` always resolves to the highest `1.x.x` stable tag.
If `1.2.3` is the latest, the channel points there. When `1.3.0` is published,
the channel automatically advances.


```graphql
type OciRepoReleaseChannel {
  name: ReleaseChannel!
  tag: Semver!
}
```




### Fields

#### [<code style={{ fontWeight: 'normal' }}>OciRepoReleaseChannel.<b>name</b></code>](#name)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>ReleaseChannel!</b></code>](/api/graphql/v1/types/scalars/release-channel.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/> \{#name\} 
The channel constraint expression (e.g., `~1`, `~1.2`, `latest`, `latest+dev`).


#### [<code style={{ fontWeight: 'normal' }}>OciRepoReleaseChannel.<b>tag</b></code>](#tag)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Semver!</b></code>](/api/graphql/v1/types/scalars/semver.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/> \{#tag\} 
The fully resolved semver version this channel currently points to.






### Member Of

[`OciRepoReleaseChannelsPage`](/api/graphql/v1/types/objects/oci-repo-release-channels-page.mdx)  <Badge class="badge badge--secondary badge--relation" text="object"/>