---
pagination_next: null
pagination_prev: null
hide_table_of_contents: true
id: release-strategy
title: ReleaseStrategy
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








Which bundle releases a package will use.


```graphql
enum ReleaseStrategy {
  STABLE
  DEVELOPMENT
}
```




### Values

#### [<code style={{ fontWeight: 'normal' }}>ReleaseStrategy.<b>STABLE</b></code>](#)  
Only use stable, published releases


#### [<code style={{ fontWeight: 'normal' }}>ReleaseStrategy.<b>DEVELOPMENT</b></code>](#)  
Use development releases for testing






### Member Of

[`Package`](/api/graphql/v1/types/objects/package.mdx)  <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`PackageLeaf`](/api/graphql/v1/types/objects/package-leaf.mdx)  <Badge class="badge badge--secondary badge--relation" text="object"/>