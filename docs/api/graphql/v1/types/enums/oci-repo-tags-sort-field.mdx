---
pagination_next: null
pagination_prev: null
hide_table_of_contents: true
id: oci-repo-tags-sort-field
title: OciRepoTagsSortField
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








Available fields for sorting tags.


```graphql
enum OciRepoTagsSortField {
  VERSION
  CREATED_AT
}
```




### Values

#### [<code style={{ fontWeight: 'normal' }}>OciRepoTagsSortField.<b>VERSION</b></code>](#version) \{#version\} 
Sort by semantic version order


#### [<code style={{ fontWeight: 'normal' }}>OciRepoTagsSortField.<b>CREATED&#x005F;AT</b></code>](#created-at) \{#created-at\} 
Sort by when the tag was published






### Member Of

[`OciRepoTagsSort`](/api/graphql/v1/types/inputs/oci-repo-tags-sort.mdx)  <Badge class="badge badge--secondary badge--relation" text="input"/>